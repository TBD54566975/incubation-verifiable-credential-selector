import * as config from '../../config';

export const mx = {
  apiKey: config.MxClientId,
  accessToken: config.MxApiSecret,
};

export const sophtron = {
  clientId: config.SophtronApiUserId,
  secret: config.SophtronApiUserSecret,
};
